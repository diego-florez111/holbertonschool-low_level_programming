#include <stdio.h>
/**
 * main - this is an example description of the function main
 */
int main(void)

{
	char string[] = "\"Programming is like building a multilingual puzzle";

	puts(string);
	/**
	 * return - the function should return a numerical value
	 */
	return (0);
}
