#include <stdio.h>
/**
 * main - this is an example description of the function main
 *
 * Return: it should return 0
 */
int main(void)

{
	char string_5[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", string_5);
	return (0);
}
